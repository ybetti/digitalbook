# Mysealake 2026年夏号 — デジタルブック

PDF を、実際の冊子のようにページをめくって読める HTML デジタルブックにしたものです。
クリックでもドラッグでも、紙をめくる 3D アニメーションでページが進みます。

ライブラリ非依存（バニラ JS / CSS のみ、外部 CDN なし）。`index.html` をブラウザで開くだけで動きます。

## 使い方

| 操作 | 方法 |
| --- | --- |
| ページをめくる | ページの左右をクリック、または端からドラッグ／スワイプ |
| 前後へ移動 | `←` `→`、マウスホイール、画面端の矢印ボタン |
| 最初／最後へ | `Home` / `End` |
| ページ一覧 | `T`（サムネイルから任意のページへジャンプ） |
| 本文検索 | `F`（PDF から抽出した本文を全ページ横断で検索） |
| 拡大／縮小 | `+` `-` `0`、`Ctrl` + ホイール、スマホはピンチ。拡大中はドラッグで移動 |
| 見開き／単ページ | `D`（狭い画面では自動で単ページ） |
| 全画面 | `E` |
| めくり音 | `S`（Web Audio で合成。音声ファイルは不要） |
| 使い方 | `?` |

そのほか

- 表示中のページは URL のハッシュ（`#p17`）に反映されるので、特定ページを共有できます
- ライト／ダークテーマを切り替えでき、設定はブラウザに保存されます
- 下部スライダーはホバーでサムネイルをプレビューします
- PDF 内のリンクは、そのままクリックできるリンクとして重ねています

## 構成

```
index.html              ビューア本体
assets/css/style.css    スタイル（テーマ・レイアウト・3D めくりの陰影）
assets/js/flipbook.js   めくりエンジン（依存ライブラリなし）
pages/full/pNNN.webp    ページ画像（横 1600px / WebP）
pages/thumb/pNNN.webp   サムネイル（横 260px / WebP）
pages/book.js           ページ数・寸法・本文テキスト・リンク座標
tools/pdf2book.py       PDF → 上記ページデータへの変換スクリプト
```

ページデータは `fetch` ではなく素の JS ファイルとして読み込むため、
ローカルサーバーを立てずに `index.html` を直接開いても動作します。

## 別の PDF で作り直す

```bash
pip install pymupdf
python tools/pdf2book.py "別の号.pdf"
```

`pages/` 以下が丸ごと差し替わり、タイトルやページ数もビューアに自動で反映されます。
主なオプション:

- `--width 2000` ページ画像の解像度（既定 1600px、拡大時の精細さと容量のバランス）
- `--quality 85` WebP 品質（既定 80）
- `--title "社内報 春号"` 表示タイトル（既定は PDF のファイル名）
- `--no-text` 本文テキストを埋め込まない（検索を無効化）
- `--no-pdf` 元 PDF へのダウンロードリンクを付けない（このリポジトリはこの設定）

## 元の PDF について

元の PDF はリポジトリに含めていません（`.gitignore` で除外）。
同梱してツールバーからダウンロードできるようにする場合は、PDF をリポジトリのルートに置き、
`--no-pdf` を付けずに変換スクリプトを実行してください。

## 公開する

静的ファイルのみなので、そのまま任意のホスティングに置けます。
GitHub Pages なら Settings → Pages で `main` ブランチのルートを指定するだけで公開できます。

## めくりの仕組み

見開きを 1 枚の「葉（leaf）」の集まりとして扱い、めくり中の 1 枚だけを
`transform-style: preserve-3d` + `rotateY()` で回転させています。
表面と裏面は `backface-visibility: hidden` で切り替わり、
回転角に応じて紙面のグラデーションと、下のページに落ちる影の濃さを連動させることで、
紙の厚みと光の当たり方を表現しています。

ドラッグ中は回転角を指の位置に追従させ、離した時点で
めくり切る／戻すのどちらかにアニメーションで収束します。
